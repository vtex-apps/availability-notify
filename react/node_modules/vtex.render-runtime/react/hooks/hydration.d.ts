/** Gets HTML content from elements rendered on the server, before being
 * hydrated on the client (and whether they have been SSRendered at all). */
export declare const useDehydratedContent: (hydrationId: string) => {
    hasRenderedOnServer: boolean;
    hasDehydratedContent: boolean;
    dehydratedElement: Element | null;
};
