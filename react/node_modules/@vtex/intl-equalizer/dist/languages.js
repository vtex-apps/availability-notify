"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getAvailableLanguages = getAvailableLanguages;

var _fs = _interopRequireDefault(require("fs"));

var _constants = require("./constants");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function getAvailableLanguages(_ref) {
  var directory = _ref.directory,
      _ref$filesToIgnore = _ref.filesToIgnore,
      filesToIgnore = _ref$filesToIgnore === void 0 ? [] : _ref$filesToIgnore;
  var languages = [];

  try {
    languages = _fs["default"].readdirSync(directory).filter(function (fileName) {
      return (fileName.includes('.json') || fileName.includes('.js')) && !filesToIgnore.includes(fileName);
    }).map(function (fileName) {
      return fileName.replace(/\.(json|js)/, '');
    });

    if (languages.length === 0) {
      return {
        error: _constants.ERRORS.ERROR_NO_LOCALE_FILES
      };
    }
  } catch (e) {
    return {
      error: _constants.ERRORS.ERROR_NO_LOCALE_FOLDER
    };
  }

  return languages.reduce(function (splittedLocales, locale) {
    if (locale.indexOf('-') > 0) {
      splittedLocales.regionLocales.push(locale);
    } else {
      splittedLocales.generalLocales.push(locale);
    }

    return splittedLocales;
  }, {
    generalLocales: [],
    regionLocales: []
  });
}