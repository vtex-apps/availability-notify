#!/usr/bin/env node
"use strict";

var _commander = _interopRequireDefault(require("commander"));

var _start = _interopRequireDefault(require("./start"));

var _fix = _interopRequireDefault(require("./fix"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

_commander["default"].option('-f, --fix').option('-a, --all', 'Show all errors.').parse(process.argv);

if (_commander["default"].fix) {
  (0, _fix["default"])();
}

if (_commander["default"].all) {
  (0, _start["default"])({
    all: true
  });
}

if (_commander["default"].args.length < 1) {
  (0, _start["default"])();
}