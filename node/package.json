{
  "name": "availability-notify",
  "version": "1.14.1",
  "description": "Record and send notification when an item is back in stock",
  "main": "index.ts",
  "scripts": {
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@vtex/api": "6.x"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "typescript": "^5.x"
  }
}
